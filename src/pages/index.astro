---
import Layout from "../layouts/Layout.astro";
import ServiceCard from "../components/ServiceCard.astro";
import ContactForm from "../components/ContactForm.astro";
import Pill from "../components/Pill.astro";
import { servicesData } from "../data/services";
import { siteConfig } from "../data/site";
import { ArrowRight, Sparkles } from "@lucide/astro";

const featuredServices = servicesData;
---

<Layout title={`${siteConfig.name} | ${siteConfig.tagline}`}>
  
  <!-- Hero Section -->
  <section class="relative min-h-[85vh] flex items-center justify-center overflow-hidden py-20 lg:py-32">
    <!-- Background Image with Dark Vignette -->
    <div class="absolute inset-0 z-0">
      <img
        src="/img/home-quad.png"
        alt="Kulexar Background"
        class="w-full h-full object-cover object-center filter brightness-[0.4] contrast-125 scale-105"
      />
      <div class="absolute inset-0 bg-gradient-to-t from-slate-950 via-slate-950/60 to-slate-950/40"></div>
    </div>

    <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center space-y-8">
      
      <!-- Top Pill Badge -->
      <Pill>
        <Sparkles class="w-4 h-4 animate-pulse text-[#DDB536]" />
	<span>Bespoke Compliance &amp; ESG Solutions</span>
      </Pill>

      <!-- Main Headline -->
      <div class="space-y-4 max-w-4xl mx-auto">
        <h1 class="text-4xl sm:text-6xl lg:text-7xl font-extrabold text-white tracking-tight leading-tight">
          Your Compliance and <br class="hidden sm:block" />
          <span class="gradient-text">Safety Experts</span>
        </h1>
        <p class="text-lg sm:text-xl text-slate-300 max-w-2xl mx-auto font-normal leading-relaxed">
          Kulexar Consulting provides Chartered professional expertise in Fire Safety, QSHE Compliance Management, and Sustainability.
        </p>
      </div>

      <!-- Action Buttons -->
      <div class="flex flex-col sm:flex-row items-center justify-center gap-4 pt-4">
        <a
          href="/services"
          class="w-full sm:w-auto px-8 py-4 rounded-full bg-[#DDB536] hover:bg-[#c89f2a] text-slate-950 font-bold text-base transition-all shadow-xl shadow-[#DDB536]/20 flex items-center justify-center gap-2 group"
        >
          <span>Explore Our Services</span>
          <ArrowRight class="w-5 h-5 group-hover:translate-x-1 transition-transform" />
        </a>
        <a
          href="/about"
          class="w-full sm:w-auto px-8 py-4 rounded-full bg-slate-900/80 hover:bg-slate-800 text-slate-200 hover:text-white border border-slate-700 font-semibold text-base transition-all backdrop-blur-md flex items-center justify-center"
        >
          <span>About Kulexar</span>
        </a>
      </div>

      <!-- Key Trust Metrics -->
      <div class="pt-12 grid grid-cols-2 md:grid-cols-4 gap-6 max-w-4xl mx-auto text-left border-t border-slate-800/80">
        <div class="space-y-1">
          <div class="text-2xl font-bold text-white text-center">Compliance</div>
          <div class="text-xs text-slate-400 text-center">Assured</div>
        </div>
        <div class="space-y-1">
          <div class="text-2xl font-bold text-white text-center">Safety</div>
          <div class="text-xs text-slate-400 text-center">Delivered</div>
        </div>
        <div class="space-y-1">
          <div class="text-2xl font-bold text-white text-center">Sustainability</div>
          <div class="text-xs text-slate-400 text-center">Solutions</div>
        </div>
        <div class="space-y-1">
          <div class="text-2xl font-bold text-white text-center">Operational</div>
          <div class="text-xs text-slate-400 text-center">Excellence</div>
        </div>
      </div>

    </div>
  </section>

  <!-- Featured Services Section -->
  <section class="py-20 bg-slate-950 border-t border-slate-900">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 space-y-12">
      
      <div class="flex flex-col md:flex-row md:items-end justify-between gap-6">
        <div class="space-y-3 max-w-2xl">
          <Pill>
            <span>Expert Capabilities</span>
          </Pill>
          <h2 class="text-3xl sm:text-4xl font-extrabold text-white tracking-tight">
            Our Specialist Solutions
          </h2>
          <p class="text-slate-400 text-base leading-relaxed">
            From fire risk assessments and site audits to QSHE compliance management and corporate sustainability.
          </p>
        </div>
        <a
          href="/services"
          class="inline-flex items-center gap-2 text-sm font-bold text-[#DDB536] hover:underline"
        >
          <span>View All Services</span>
          <ArrowRight class="w-4 h-4" />
        </a>
      </div>

      <!-- Services Grid -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {
          featuredServices.map((service) => (
            <ServiceCard
              title={service.title}
              description={service.shortDescription}
              image={service.cardImage}
              slug={service.slug}
              iconName={service.iconName}
            />
          ))
        }
      </div>

    </div>
  </section>

  <!-- Quick Contact Section -->
  <section class="py-20 bg-slate-950">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <ContactForm />
    </div>
  </section>

</Layout>
